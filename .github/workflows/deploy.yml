name: Deploy Pipeline

on:
  push:
    branches: [ "main" ]
  workflow_run:
    workflows: ["FlowForge AI CI"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: $\{{ github.event.workflow_run.conclusion == 'success' \}}
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up kubectl
      uses: azure/setup-kubectl@v3
      with:
        version: 'latest'
        
    - name: Deploy to Kubernetes
      run: |
        echo "Deploying to Kubernetes cluster..."
        # In a real environment, you would authenticate to your cluster here
        # kubectl apply -f k8s/
